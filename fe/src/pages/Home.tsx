import BookDisplay from "../components/BookDisplay/BookDisplay";

const Home = () => {
    return (
        <>  
            {/* <FilterDisplay/> */}
            <BookDisplay/>
        </>
    );
};

export default Home;
